# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/personalmoneymanager
spring.datasource.username=root
spring.datasource.password=password

# Server configuration
server.servlet.context-path=/api/v1

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Brevo SMTP configuration (port 465 SSL — Render blocks port 587)
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=465
spring.mail.username=${BREVO_USERNAME_JAVA_APP}
spring.mail.password=${BREVO_APP_PASSWORD_JAVA_APP}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.starttls.enable=false
spring.mail.protocol=smtps
spring.mail.properties.mail.smtp.from=${BREVO_EMAIL_JAVA_APP}

jwt.secret=2b8e7c7e-4e2a-4b1a-9c2e-8f7e2d3c4b5a


financemanager.frontend.url=${FINANCEMANAGER_FRONTEND_URI}

spring.profiles.active=prod

financemanager.activation.uri=${FINANCEMANAGER_BACKEND_URI}